import React from "react";

import { HeaderContainer, Logo } from "./styles";

const Header = () => {
  return (
    <HeaderContainer>
      <Logo>Glickr</Logo>
    </HeaderContainer>
  );
};

export default Header;
